/**
 * DEAD CODE — This component is not used anywhere in the app.
 * The alignment buttons are rendered inline in app/page.tsx
 * inside the mobile controls row (lines ~88–148).
 *
 * TODO: Either delete this file, or refactor page.tsx to use it
 *       and remove the inline button blocks.
 */

export {};
