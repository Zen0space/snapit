/**
 * DEAD CODE — This component is not used anywhere in the app.
 * The Padding slider lives in ShadowControls.tsx (Appearance section).
 * Canvas mode (ratio/manual) is controlled via AspectRatioPanel.tsx.
 *
 * TODO: Delete this file.
 */

export {};
